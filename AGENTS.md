# Pico W sensor

- 日本語で説明する。
- firmware/ が本体プログラムの編集元。config.py の秘密情報を出力・Git登録しない。
- Python標準ライブラリだけで開発・テスト・転送する。パッケージをグローバルにインストールしない。
- 変更後は `python3 pico.py test`。ハードウェア依存の変更は対象Picoで検証し、未検証範囲を明示する。
- 本体への反映には `python3 pico.py deploy` を使う。独自のシリアル書き込みは避ける。
- deploy / backup / restore は計測を一時中断し、終了時に本体を再起動する。
- USB転送中のウォッチドッグ対策、書き込み前バックアップ、照合、失敗時の復元処理を維持する。
- backups/ は秘密情報を含む。権限を維持し、外部に送らない。
- Discord送信を伴う実機検証では送信回数を必要最小限にする。
